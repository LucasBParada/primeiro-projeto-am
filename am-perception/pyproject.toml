[project]
name = "am-perception"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "matplotlib>=3.11.1",
    "pydantic>=2.13.4",
]

[project.scripts]
am-perception = "am_perception:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
